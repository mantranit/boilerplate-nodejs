{
  "swaggerDefinition": {
    "info": {
      "title": "Change4Change API on HEROKU",
      "version": "1.0.0",
      "description": "RESTful API for Change4Change"
    },
    "host": "c4c-be.herokuapp.com",
    "basePath": "/api",
    "schemes": ["https"],
    "consumes": ["application/x-www-form-urlencoded"],
    "produces": ["application/json"],
    "securityDefinitions": {
      "Bearer": {
        "description": "For accessing the API a valid JWT token must be passed in all the queries in the 'Authorization' header. \n\nA valid JWT token is generated by the API and returned as answer of a call to the route /authenticate giving a valid email & password. \n\nThe following syntax must be used in the 'Authorization' header : Bearer: xxxxxx.yyyyyyy.zzzzzz\n\n",
        "type": "apiKey",
        "name": "Authorization",
        "in": "header"
      }
    }
  },
  "apis": ["./api/**/*.js"]
}
